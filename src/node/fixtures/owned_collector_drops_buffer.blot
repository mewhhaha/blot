open import "blot:prelude"
const Collect = import "../../../examples/lib/owned_collector.blot"

const replace :: Collect.Collector (Int, Int)
const replace = fn (?values, input) => #Continue [input]

return Collect.run replace [1, 2]
