open import "blot:prelude"
const W = import "../../../examples/lib/subtyping_witness.blot"

const Internal = { .path = Text; .trace = Text; }
const Public = { .path = Text; }

// A missing field cannot be created by a no-op conversion. The generated
// Source -> Target signature must reject this direction at compile time.
const invalid = W.derive (Public, Internal)
return invalid
