// Generated by generate.ts --components=4 --systems=8 --stages=2 --sink=build-only.
// Do not hand-edit: regenerate with the recorded parameters instead.
open import "blot:prelude"
const Framework = import "../framework.blot"

// Bind-then-apply: framework functions are bound to local consts before
// application (direct application through an import record projection
// mis-checks polymorphic definitions; see cold-semantic README).
// Functions applied to distinct row types are projected once per world:
// a single projection binding does not generalize across type arguments.
const AppNew = Framework.App.new
const AppAddPlugin = Framework.App.add_plugin
const AppAddComponent = Framework.App.add_component
const AppAddSystem = Framework.App.add_system
const PlanGroup = Framework.Plan.group
const PlanBarrier = Framework.Plan.barrier
const PlanThen = Framework.Plan.then
const PlanBefore = Framework.Plan.before
const ForceUnite = Framework.Force.unite

const flag_value = fn flag => case flag of
  #True => 1
  #False => 0

const app0 = AppNew ()
const app1 = AppAddPlugin (app0, { .name = "core"; .priority = 0; })
const app2 = AppAddPlugin (app1, { .name = "telemetry"; .priority = 1; .labels = ["cold", "semantic"]; })
const app3 = AppAddComponent (app2, { .name = "C0"; .Type = Int; .seed = 0; })
const app4 = AppAddComponent (app3, { .name = "C1"; .Type = { .x = Int; .y = Int; }; .seed = { .x = 0; .y = 0; }; })
const app5 = AppAddComponent (app4, { .name = "C2"; .Type = Text; .seed = "seed"; })
const app6 = AppAddComponent (app5, { .name = "C3"; .Type = Bool; .seed = False; })
const app7 = AppAddSystem (app6, { .name = "s0"; .read = "C0"; .write = "C0"; .read_type = Int; .write_type = Int; })
const app8 = AppAddSystem (app7, { .name = "s1"; .read = "C1"; .write = "C1"; .read_type = { .x = Int; .y = Int; }; .write_type = { .x = Int; .y = Int; }; })
const app9 = AppAddSystem (app8, { .name = "s2"; .read = "C2"; .write = "C2"; .read_type = Text; .write_type = Text; })
const app10 = AppAddSystem (app9, { .name = "s3"; .read = "C3"; .write = "C3"; .read_type = Bool; .write_type = Bool; })
const app11 = AppAddSystem (app10, { .name = "s4"; .read = "C0"; .write = "C0"; .read_type = Int; .write_type = Int; })
const app12 = AppAddSystem (app11, { .name = "s5"; .read = "C1"; .write = "C1"; .read_type = { .x = Int; .y = Int; }; .write_type = { .x = Int; .y = Int; }; })
const app13 = AppAddSystem (app12, { .name = "s6"; .read = "C2"; .write = "C2"; .read_type = Text; .write_type = Text; })
const app14 = AppAddSystem (app13, { .name = "s7"; .read = "C3"; .write = "C3"; .read_type = Bool; .write_type = Bool; })

const BuildRow_a = Framework.Build.row
const SystemsFn_a = Framework.Systems
const PlanCompile_a = Framework.Plan.compile
const SchedFn_a = Framework.Schedule
const ForceForWorld_a = Framework.Force.for_world
const world_a = BuildRow_a (app14, "a")
const Row_a = world_a.Row

const BuildRow_b = Framework.Build.row
const SystemsFn_b = Framework.Systems
const PlanCompile_b = Framework.Plan.compile
const SchedFn_b = Framework.Schedule
const ForceForWorld_b = Framework.Force.for_world
const world_b = BuildRow_b (app14, "b")
const Row_b = world_b.Row

const BuildRow_c = Framework.Build.row
const SystemsFn_c = Framework.Systems
const PlanCompile_c = Framework.Plan.compile
const SchedFn_c = Framework.Schedule
const ForceForWorld_c = Framework.Force.for_world
const world_c = BuildRow_c (app14, "c")
const Row_c = world_c.Row

const Systems_a = SystemsFn_a Row_a
const registry_a = {
  .s0 = Systems_a.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s1 = Systems_a.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s2 = Systems_a.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s3 = Systems_a.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
  .s4 = Systems_a.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s5 = Systems_a.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s6 = Systems_a.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s7 = Systems_a.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
}

const seed_a: (Int, Int) -> [Row_a]
const seed_a = fn (count, offset) => Iter.collect (Iter.map (
  Iter.range (0, count),
  fn index => {
    .which = "a";
    .C0 = index + offset;
    .C1 = { .x = index; .y = index + offset; };
    .C2 = "r" <> Text.of_int index;
    .C3 = index == offset;
  }
))

const render_a = fn row => row.C0 + row.C1.x * 5 + row.C1.y * 7 + Text.length row.C2 + flag_value row.C3
const checksum_a: [Row_a] -> Int
const checksum_a = fn rows => fold (rows, 0, fn (total, row) => total + render_a row)

const Sched_a = SchedFn_a Row_a
const tick1_a = Sched_a.merge (registry_a.s0.step, Sched_a.merge (registry_a.s1.step, Sched_a.merge (registry_a.s2.step, Sched_a.merge (registry_a.s3.step, Sched_a.merge (registry_a.s4.step, Sched_a.merge (registry_a.s5.step, Sched_a.merge (registry_a.s6.step, registry_a.s7.step)))))))

const Loop_a = ForceForWorld_a Row_a
const initial_a: Unit -> Row_a
const initial_a = fn () => {
    .which = "a";
    .C0 = 0;
    .C1 = { .x = 0; .y = 0; };
    .C2 = "seed";
    .C3 = False;
  }
const game_a = {
  .initial = initial_a;
  .update = fn (row, step) => tick1_a row;
  .render = render_a;
}
const run_a = Loop_a.run game_a

const Systems_b = SystemsFn_b Row_b
const registry_b = {
  .s0 = Systems_b.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s1 = Systems_b.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s2 = Systems_b.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s3 = Systems_b.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
  .s4 = Systems_b.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s5 = Systems_b.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s6 = Systems_b.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s7 = Systems_b.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
}

const seed_b: (Int, Int) -> [Row_b]
const seed_b = fn (count, offset) => Iter.collect (Iter.map (
  Iter.range (0, count),
  fn index => {
    .which = "b";
    .C0 = index + offset;
    .C1 = { .x = index; .y = index + offset; };
    .C2 = "r" <> Text.of_int index;
    .C3 = index == offset;
  }
))

const render_b = fn row => row.C0 + row.C1.x * 5 + row.C1.y * 7 + Text.length row.C2 + flag_value row.C3
const checksum_b: [Row_b] -> Int
const checksum_b = fn rows => fold (rows, 0, fn (total, row) => total + render_b row)

const Sched_b = SchedFn_b Row_b
const tick1_b = Sched_b.merge (registry_b.s0.step, Sched_b.merge (registry_b.s1.step, Sched_b.merge (registry_b.s2.step, Sched_b.merge (registry_b.s3.step, Sched_b.merge (registry_b.s4.step, Sched_b.merge (registry_b.s5.step, Sched_b.merge (registry_b.s6.step, registry_b.s7.step)))))))

const Loop_b = ForceForWorld_b Row_b
const initial_b: Unit -> Row_b
const initial_b = fn () => {
    .which = "b";
    .C0 = 0;
    .C1 = { .x = 0; .y = 0; };
    .C2 = "seed";
    .C3 = False;
  }
const game_b = {
  .initial = initial_b;
  .update = fn (row, step) => tick1_b row;
  .render = render_b;
}
const run_b = Loop_b.run game_b

const Systems_c = SystemsFn_c Row_c
const registry_c = {
  .s0 = Systems_c.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s1 = Systems_c.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s2 = Systems_c.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s3 = Systems_c.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
  .s4 = Systems_c.define (
    { .reads = [{ .name = "C0"; .Type = Int; }]; .writes = [{ .name = "C0"; .Type = Int; }]; },
    fn read => { .C0 = read.C0 + 1; }
  );
  .s5 = Systems_c.define (
    { .reads = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; .writes = [{ .name = "C1"; .Type = { .x = Int; .y = Int; }; }]; },
    fn read => { .C1 = { .x = read.C1.x + read.C1.y; .y = read.C1.x - read.C1.y; }; }
  );
  .s6 = Systems_c.define (
    { .reads = [{ .name = "C2"; .Type = Text; }]; .writes = [{ .name = "C2"; .Type = Text; }]; },
    fn read => { .C2 = read.C2 <> "!"; }
  );
  .s7 = Systems_c.define (
    { .reads = [{ .name = "C3"; .Type = Bool; }]; .writes = [{ .name = "C3"; .Type = Bool; }]; },
    fn read => { .C3 = not (read.C3); }
  );
}

const seed_c: (Int, Int) -> [Row_c]
const seed_c = fn (count, offset) => Iter.collect (Iter.map (
  Iter.range (0, count),
  fn index => {
    .which = "c";
    .C0 = index + offset;
    .C1 = { .x = index; .y = index + offset; };
    .C2 = "r" <> Text.of_int index;
    .C3 = index == offset;
  }
))

const render_c = fn row => row.C0 + row.C1.x * 5 + row.C1.y * 7 + Text.length row.C2 + flag_value row.C3
const checksum_c: [Row_c] -> Int
const checksum_c = fn rows => fold (rows, 0, fn (total, row) => total + render_c row)

const Sched_c = SchedFn_c Row_c
const tick1_c = Sched_c.merge (registry_c.s0.step, Sched_c.merge (registry_c.s1.step, Sched_c.merge (registry_c.s2.step, Sched_c.merge (registry_c.s3.step, Sched_c.merge (registry_c.s4.step, Sched_c.merge (registry_c.s5.step, Sched_c.merge (registry_c.s6.step, registry_c.s7.step)))))))

const Loop_c = ForceForWorld_c Row_c
const initial_c: Unit -> Row_c
const initial_c = fn () => {
    .which = "c";
    .C0 = 0;
    .C1 = { .x = 0; .y = 0; };
    .C2 = "seed";
    .C3 = False;
  }
const game_c = {
  .initial = initial_c;
  .update = fn (row, step) => tick1_c row;
  .render = render_c;
}
const run_c = Loop_c.run game_c

const tick_a = Sched_a.each Sched_a.empty
const tick_b = Sched_b.each Sched_b.empty
const tick_c = Sched_c.each Sched_c.empty

const run: Int -> Int
const run = fn (count: Int) -> Int => checksum_a (tick_a (seed_a (count, 0))) + checksum_b (tick_b (seed_b (count, 0))) + checksum_c (tick_c (seed_c (count, 0)))

return { .run; }
