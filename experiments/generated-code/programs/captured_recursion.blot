open import "blot:prelude"

sig count_to = Int -> Int
const count_to = fn limit => do:
  sig go = (Int, Int) -> Int
  let rec go = fn (index, total) => do:
    if index < limit:
      return go (index + 1, total + index)

    return total
  return go (0, 0)

return { .count_to = count_to; }
