open import "blot:prelude"

const sum_to :: Int -> Int
const sum_to = fn count => do:
  let total = 0
  for n in Iter.range (0, count):
    total := total + n

  return total

return { .sum_to = sum_to; }
