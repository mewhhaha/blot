// The fresh Array is handed directly to the consuming quicksort contract.
// There is no copy boundary and no ownership syntax at the call site.

open import "blot:prelude"

let values = [4, 7, 3, 8, 2, 6, 1, 5]
return Array.quicksort (values, fn (left, right) => left <= right)
