// Duplicated callback aliases must not disappear during exact conversion.
open import "blot:prelude"
const increment = fn number => @int.add number 1
const callbacks = [increment, increment]
const call = @array.get callbacks 1
let result :: Int
let result = call 41
return result
