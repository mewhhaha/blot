open import "blot:prelude"
const example = import "./lib/collection_effects.blot"

return @handle (
  example.Tick,
  fn () => example.run [1, 2, 3],
  { .next = fn (value, ?resume) => resume (value * 2); .return = identity; }
  )
