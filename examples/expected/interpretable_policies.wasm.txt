{ .admin = { .allowed = true; .trace = { .allowed = true; .notes = ["+admin", "-region", "-quota<=80"] } }; .member = { .allowed = true; .trace = { .allowed = true; .notes = ["-admin", "+region", "+quota<=80"] } }; .over_limit = { .allowed = false; .trace = { .allowed = false; .notes = ["-admin", "+region", "-quota<=80"] } }; .wrong_region = { .allowed = false; .trace = { .allowed = false; .notes = ["-admin", "-region", "+quota<=80"] } } }
