{ .boundary = { .retry_limit = 5; .status = "ok"; .timeout_ms = 60000 }; .invalid_retry = { .retry_limit = 8; .status = "retry"; .timeout_ms = 0 }; .invalid_seconds = { .retry_limit = 0; .status = "seconds"; .timeout_ms = 90 }; .valid = { .retry_limit = 3; .status = "ok"; .timeout_ms = 30000 } }
