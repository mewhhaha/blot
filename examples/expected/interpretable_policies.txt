{ .default = { .admin = { .allowed = #True; .trace = { .allowed = #True; .notes = ["+admin", "-region", "-quota<=80"]; }; }; .member = { .allowed = #True; .trace = { .allowed = #True; .notes = ["-admin", "+region", "+quota<=80"]; }; }; .over_limit = { .allowed = #False; .trace = { .allowed = #False; .notes = ["-admin", "+region", "-quota<=80"]; }; }; .wrong_region = { .allowed = #False; .trace = { .allowed = #False; .notes = ["-admin", "-region", "+quota<=80"]; }; }; }; }
