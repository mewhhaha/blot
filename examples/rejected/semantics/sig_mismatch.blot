// A `sig` is checked by subsumption. It is an ordinary expression evaluated at
// compile time, bridged into the lattice, and constrained against what the
// binding actually infers.

open import "blot:prelude"

sig double = Int -> Int
let double = fn value => @text.concat value "!"
return double
