// Ordinary application. Nothing here is annotated; the constraint comes from
// `@int.add`'s own type flowing backwards into the parameter.

open {} = (@import "blot:prelude") ();

let increment = fn x => @int.add x 1;
return increment "text";
