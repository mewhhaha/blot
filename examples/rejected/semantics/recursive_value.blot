// A recursive group's names are in scope from the first member onward, but
// none of them holds a value until every member is bound. A function body can
// wait for that; a sum cannot, so a recursive binding accepts only a lambda.

open import "blot:prelude"

let rec total = later 1
let rec later = fn n => n + 1
return total
