open import "blot:prelude"

let apply_identity ::
  @forall (fn T => T -> T) -> { .number = Int; .text = Text; }
let apply_identity = fn identity => {
  .number = identity 42;
  .text = identity "forty-two";
}
let increment = fn value => @int.add value 1
return apply_identity increment
