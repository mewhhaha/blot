// Equal public names do not make different invariant carriers the same seal.

open import "blot:prelude"

const IntegerBox =
  seal ("Box", I32) <+ { .of = fn inner => seal ("Box", inner); }

let value :: IntegerBox
let value = IntegerBox.of "text"

return value
