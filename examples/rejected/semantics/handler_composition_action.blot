// A composition step is the two-argument surface spelling of `@handle`.
// Another intrinsic does not identify a source handler to compose.

return try (() => ()) then do
  @not_handle (MissingEffect, missing_handler)
end;
