// Storing a linear closure would make the shape linear, and blot does not track
// linear structures yet. Saying so beats losing the obligation quietly.

open {} = (@import "blot:prelude") ();

let consume = fn !value => @int.add value 1;
let !token = 41;
return { .go = fn () => consume (!token); };
