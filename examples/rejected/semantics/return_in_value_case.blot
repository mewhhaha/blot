open {} = @import "blot:prelude" ();

let unwrap = option => do
  let value = case option of
    #Some inner => do
      return inner;
    end,
    #None => 0
  end;
  in value
end;

return unwrap (Some 1);
