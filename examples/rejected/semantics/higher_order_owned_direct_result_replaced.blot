open import "blot:prelude"

const carry = fn transition => fn ?state => do:
  ?next <- transition (?state)
  return next

const replace = fn ?state => [1, 2, 3]

let state = [4, 5, 6]
return carry replace state
