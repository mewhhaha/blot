// Every accumulator slot keeps one stable type across iterations.
open @import "blot:prelude" ();

let total = 0;
for Iter.range (0, 3) do
  total := "not a number";
  total := total + 1;
end;

return total;
