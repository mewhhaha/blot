// Consumed on one path and not the other is consumed neither exactly once nor
// never, so the branches have to agree.
let !token = 41;
let consume = (!value) => value;
return if 1 < 2 then consume (!token) else 0 end;
