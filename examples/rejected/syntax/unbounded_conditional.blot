// Every variable-width region carries an explicit terminator. A conditional
// without `end` has no locatable boundary, so it cannot be an island.
let label = if 1 == 1 then "yes" else "no";
return label;
