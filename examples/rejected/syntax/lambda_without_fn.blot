// `fn` is the only way to write a lambda. The arrow is reserved for the
// parameter boundary, so omitting `fn` is rejected during parsing.

open import "blot:prelude"

let apply_twice = f => x => f (f x)
return apply_twice
