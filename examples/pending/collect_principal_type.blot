// NOT IMPLEMENTED YET (TASKS.md §2): `collect` evaluates to an integer array,
// but its inferred result still retains the unconstrained empty-accumulator
// alternative. The test records that non-principal type directly. Once the
// produced element closes the alternative, this file should infer `[Int]` and
// be promoted to the supported catalog.

open import "blot:prelude"

let collected = collect (Iter.range (0, 4))
return collected
