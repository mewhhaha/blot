// Conjunction intersects disconnected sets without filling their holes.

open import "blot:prelude"

const Allowed = refine (Int, fn value => (value == 0 || value == 4 || value == 8) && (value == 4 || value == 8 || value == 12))
let chosen :: Allowed
let chosen = 4
return chosen
