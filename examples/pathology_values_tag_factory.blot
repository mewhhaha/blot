// A compile-time factory may return a tagged callback.
open import "blot:prelude"
const increment = fn number => @int.add number 1
const box = (fn callback => #Some callback) increment
let result :: Int
let result = case box of
  #Some call => call 41
return result
