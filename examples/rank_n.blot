open import "blot:prelude"

let use :: @forall (fn T => T -> T) -> { .number = Int; .text = Text; }
let use = fn identity => {
  .number = identity 42;
  .text = identity "forty-two";
}
let identity = fn value => value
return use identity
