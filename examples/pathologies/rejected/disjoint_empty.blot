// Expected at check: BLOT_EMPTY_REFINEMENT

open import "blot:prelude"

const Allowed =
  refine (
    Int,
    fn value => (value == 0 || value == 4) && (value == 2 || value == 6)
  )
return Allowed
