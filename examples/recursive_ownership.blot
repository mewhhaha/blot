open import "blot:prelude"

let consume = fn !value => value + 1
let !token = 41
let rec even =
  fn n => do:
    if n < 1:
      return consume (!token)
    else:
      return odd (n - 1)
let rec odd = fn n => even (n - 1)

return even 4
