open {} = (@import "blot:prelude") ();

let consume = fn !value => value + 1;
let !token = 41;
let even = rec (fn n =>
  if n < 1 then consume (!token) else odd (n - 1) end);
let odd = rec (fn n => even (n - 1));

return even 4;
