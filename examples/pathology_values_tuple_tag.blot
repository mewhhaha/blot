// A tuple field retains a callable constructor payload.
open import "blot:prelude"
const increment = fn number => @int.add number 1
const pair = (#Some increment, 1)
let result :: Int
let result = case pair.0 of
  #Some call => call 41
return result
