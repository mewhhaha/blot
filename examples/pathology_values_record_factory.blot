// A compile-time factory preserves callable record fields.
open import "blot:prelude"
const increment = fn number => @int.add number 1
const box = (fn callback => { .call = callback; }) increment
let result :: Int
let result = box.call 41
return result
