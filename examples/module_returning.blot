// A module is a return scope. Its standalone statement control flow retains
// that target because an `if` does not establish another one.

open import "blot:prelude"

if 1 < 2:
  return 42

export 0
