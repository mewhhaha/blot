// Structural functions specialize at each concrete call shape.

open {} = (@import "blot:prelude") ();

let get_x = fn value => value.x;

return get_x { .x = 1; .y = 2; } + get_x { .x = 3; .z = 4; };
