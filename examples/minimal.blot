// The smallest legal module: no module or fixity header, one result.
//
// Even this opens the prelude. It has no privilege — a module that does not
// open it has no `Int.add` binding in scope to answer the standard source
// declaration for `+`.

open import "blot:prelude"

return 42
