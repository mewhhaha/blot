name: remove-retired-surface-inspect

on:
  push:
    branches:
      - agent/remove-retired-surface-syntax

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Find retired surface references
        run: |
          printf '%s\n' '--- production code references ---'
          grep -R -n -E 'handler_composition|BLOT_VALUE_IF_REMOVED|BLOT_TRY_REMOVED|discarded-value-if|discardedValueIf|isValueConditional' \
            --exclude-dir=.git --exclude-dir=migration --exclude='parser.plan' . || true
          printf '%s\n' '--- documentation references ---'
          grep -R -n -E 'expression `if`|value conditional|value-producing `if`|try .*with|`try|handler composition' \
            README.md LANGUAGE.md AGENTS.md DOCS.md docs spec || true
          printf '%s\n' '--- formatter references ---'
          grep -n -E 'conditional|handler_composition' src/tooling/formatter.ts || true
